/*package com.example.wishlist

class ListFetcher {
        companion object {
            val price =
            val itemName = listOf()
            val URL = listOf()

            fun getItems(): MutableList<Item> {
                var Items: MutableList<Item> = ArrayList()
                for (i in 0..9) {
                    val item = Item(price[i], itemName[i], URL[i])
                    itemName.add(item)
                }
                return itemName
            }

            fun addItem(): MutableList<Item> {
                var newItem: MutableList<Item> = ArrayList()
                for (i in 10..14) {
                    val item = Item(price[i], itemName[i], URL[i])
                    newItem.add(item)
                }
                return newItem
            }

        }
}
*/
